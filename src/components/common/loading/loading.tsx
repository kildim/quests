import * as S from './loading.styled';

const Loading = () => (
      <S.BlockLayer>
        <S.Modal>
          <S.ModalText>Loading ...</S.ModalText>
        </S.Modal>
      </S.BlockLayer>
);

export default Loading;
